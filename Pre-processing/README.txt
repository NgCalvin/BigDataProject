The python class have function precess(line).
It simply take in a line with format like the reviews.csv and process it so it will return <rating,summary,comment> in String.
Comment part will be lower case and strip away punctation and stopwords.